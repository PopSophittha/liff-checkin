//use web api in GAS

// function doGet() {
//   return HtmlService.createHtmlOutputFromFile("Index");
// }

// function saveLocation(name, status, lat, lng, note) {
//   const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('WorkingFile');
//   const now = new Date();

//   sheet.appendRow([now, "", name, status, lat, lng, note]);

//   // ไม่ต้อง return ข้อความ — ส่งสถานะผ่าน JS ดีกว่า
//   return status; // ส่งแค่ status กลับมา เช่น "Check In"
// }
